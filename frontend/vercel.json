{
  "framework": "vite",
  "buildCommand": "rm -rf content && git clone --depth 1 https://github.com/omkar142web/learning-curriculum content && npm install && npm run build",
  "outputDirectory": "dist",
  "functions": {
    "api/index.js": {
      "includeFiles": "content/**"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
