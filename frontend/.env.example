# Where the API function finds Markdown content (serverless bundle).
# Set this on Vercel:
#   CONTENT_DIR=./content
# Local default (when unset): ../../content  → e-learning/content
#
# VITE_API_URL is no longer needed: the API runs on the same domain
# as the site (/api), so the frontend calls it with a relative path.