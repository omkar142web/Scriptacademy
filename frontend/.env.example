# Base URL of the backend API.
# Local default (when unset): /api → Vite proxy → http://localhost:5000
# On Vercel: point at your Render backend, e.g. https://your-backend.onrender.com/api
VITE_API_URL=https://your-backend.onrender.com/api